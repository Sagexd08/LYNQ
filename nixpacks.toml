# Nixpacks config for Railway deployment
# IMPORTANT: Set Railway root directory to "backend" in service settings
# This file is for reference - Railway will use backend/nixpacks.toml when root is set to backend
# If Railway root is NOT set to backend, this config will be used

[phases.setup]
nixPkgs = ["nodejs_24", "openssl"]

[phases.install]
# Use npm install which works with or without package-lock.json
# npm install will create/update package-lock.json if needed
cmds = ["cd backend && npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "cd backend && npm run build",
  "cd backend && npm run prisma:generate",
  "cd backend && npx prisma migrate deploy"
]

[start]
cmd = "cd backend && npm run start:prod"
